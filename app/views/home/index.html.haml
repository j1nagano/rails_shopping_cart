%h1 商品一覧

%table
  %thead
    %tr
      %th 商品画像
      %th 商品名
      %th 説明文
      %th 価格
      %th

  %tbody
    - @items.each do |item|
      %tr
        %td 画像
        %td= item.name
        %td= item.description
        %td= number_to_currency( item.price, :precision => 0, :unit => '￥' )
        %td= link_to 'カートに入れる', {:controller => 'home', :action => 'index'}, {:data => item}
